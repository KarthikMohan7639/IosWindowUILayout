I have created a high-fidelity macOS Finder UI concept using v0, but I need your clarification on several key points before I finalize the implementation plan. Please answer the following:

Clarification for better understanding: It is just an UI running on top of web application while is IOS UI should the this web application resembles like standalone application but not it still runs on web application . Basically I’m asking to design an IOS window layout inside an website

Window Controls: o Close (Red): Should this hide the window component from the view? If so, how should the user re-open it? (e.g., via a desktop icon?) Answer: If the user clicks close then the application should close and there should appear an icon in the application itself if user clicks the icon the application should open o Minimize (Yellow): Should this shrink the window to a dock/bar at the bottom? Answer: Yes it should shrink in the website itself o Maximize (Green): Should this toggle between the current size and filling the browser viewport? Answer: yes the viewport the web applicatipon
AI Chat (Dialogflow): o Do you have a Google Cloud Project ID and Client Email for Dialogflow? (Do not share the private key here; I'll guide you on setting it up in Render). o If no, should I use mock AI responses for now? use mock ai for time being
Search Functionality: o "Search by meaning" implies semantic search. Do you want a full implementation (requires OpenAI API key + Vector DB) or a keyword/tag-based search for this version? search functionality involves the loaded files and folders like that only it should search that only
Folder Data: o Should folders/files be stored in MongoDB? No the loaded folders is local to the web application which is running store the data in browser local storage itself and delete for each session o Do you need "Create/Delete" functionality or just navigation through pre-loaded data? yes needed the create delete functionality button when right click
Deployment: o I will set up for Render. Do you prefer a render.yaml file or manual instructions? o Yes Note: It should resemble the ios which running on top of web application